<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <h2>Original and shortened URLs</h2>
<ul>
    <% for (var url in urls) { %>
        <li><%= url %> - <%= urls[url] %>             <form method="POST" action=<%= "/urls/" + url + "/delete"%> > <button type="submit">Delete URL</button></form> </li>
    <% }; %>
</ul>
<a href=''> Create new TinyURL </a>
</body>
</html>